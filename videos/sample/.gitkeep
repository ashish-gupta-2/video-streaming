# This directory should contain:
# - index.m3u8 (HLS playlist file)
# - segment0.ts, segment1.ts, etc. (video segments)

# To convert a video to HLS format, use FFmpeg:
# ffmpeg -i input.mp4 -codec: copy -start_number 0 -hls_time 10 -hls_list_size 0 -f hls index.m3u8

